# Arrays Examples

### Array declaration
let nums = [1, 2, 3, 4, 5];
log(nums)

### Array elements can be any legal expression
let add = fn(x, y) { x + y };
let arr = ["string", false, 10, add(nums[0], nums[len(nums) - 1]), nums]
log(arr)

### Arrays are indexed and can be accessed via the index expression
log(arr[2])

### Multidemensional Arrays are also accessible via its index
log(arr[4][2])
