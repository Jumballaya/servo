let filter = fn(arr, f) {
  let iter = fn(arr, acc) {
    if (len(arr) == 0) {
      acc;
    } else {
      if (f(first(arr))) {
        iter(rest(arr), push(acc, first(arr)))
      } else {
        iter(rest(arr), acc)
      }
    }
  };

  iter(arr, [])
};
