import nums from './arrays.svo';
import closure from './functions.svo';
import message from './strings.svo';
import map from '../stdlib/.svo/Array.svo';

let addTen = closure(10);

log(nums);
log(map(nums, addTen));
log(message);
